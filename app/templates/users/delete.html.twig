{% include 'default/_form.html.twig' with {
    page_title: 'title.users_delete'|trans({'%id%': users.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'users_index',
} %}