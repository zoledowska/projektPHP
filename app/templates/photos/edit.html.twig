{% include 'default/_form.html.twig' with {
    page_title: 'title.photos_edit'|trans({'%id%': photos.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'photos_index',
} %}