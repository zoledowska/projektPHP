{% include 'default/_form.html.twig' with {
    page_title: 'title.photos_delete'|trans({'%id%': comments.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'photos_index',
} %}