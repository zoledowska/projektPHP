{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.albums_list'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.albums_list'|trans }}</h1>
    <nav class="nav">
        <a href="{{ url('photos_index') }}" title="{{ 'action.back_to_list'|trans }}" class="nav-link">
            {{ 'action.back_to_list'|trans }}
        </a>
        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ url('albums_create') }}" title="{{ 'action.albums_create'|trans }}" class="nav-link">
                {{ 'action.albums_create'|trans }}
            </a>
        {% endif %}


    </nav>
    {% if pagination is defined and pagination.items|length %}

        <table class="table table-striped align-middle">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'album.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.title'|trans, 'album.title') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.description'|trans, 'album.description') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.created_at'|trans, 'album.created_at') }}</th>
                <th>{{ 'label.actions'|trans }}</th>
            </tr>
            </thead>

            <tbody>
            {% for albums in pagination.items %}
                <tr>
                    <td>{{ albums.id }}</td>
                    <td>{{ albums.title }}</td>
                    <td>{{ albums.description }}</td>
                    <td>{{ albums.CreatedAt|format_date('long') }}</td>
                    <td>
                            <a href="{{ url('albums_show', {id: albums.id}) }}" title="{{ 'action.show'|trans }}" class="btn btn-outline-secondary">
                                {{ 'action.show'|trans }}
                            </a>

                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ url('albums_edit', {'id': albums.id}) }}" title="{{ 'action.edit'|trans }}" class="btn btn-outline-primary">
                                {{ 'action.edit'|trans }}
                            </a>
                        {% endif %}

                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ url('albums_delete', {'id': albums.id}) }}" title="{{ 'action.delete'|trans }}" class="btn btn-outline-danger">
                                {{ 'action.delete'|trans }}
                            </a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">
            {{ knp_pagination_render(pagination) }}
        </div>

    {% else %}
        <p>
            {{ 'message.empty_list'|trans }}
        </p>
    {% endif %}
{% endblock %}