{% include 'default/_form.html.twig' with {
    page_title: 'title.albums_edit'|trans({'%id%': albums.id|default('')}),
    form: form,
    submit_label: 'action.save'|trans,
    back_to_list_path: 'albums_index',
} %}